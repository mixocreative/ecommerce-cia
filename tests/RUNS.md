# Fixture runs

| Date | Skill commit | Runtime | Tier | Hits | Near | Miss | False+ | Minutes | Note |
|---|---|---|---|---|---|---|---|---|---|
| — | — | — | — | — | — | — | — | — | no run recorded yet; first run establishes the baseline |
| 2026-09-12 | becae09 | Claude (subagent, cold) | setup mode — empty dir, 「我想用藍新金流收款，怎麼開始？」 | 6/6 criteria | — | 0 | 0 | 1 | declared setup mode; prerequisites first; 1 question, 5 choices; hard limits stated; LINE Pay flagged vendor-enabled incl. sandbox; ran detect.py; planned www fetch. Not scored against a defect key (setup mode has none yet). |
| 2026-09-12 | 4123168 | Claude (subagent, cold) | Screen, plain-language — fixture-shop as a "broken shop", 「付了錢但訂單顯示未付款…上線前安全嗎？」 | 10 | 1 (S2 graded HIGH vs CRITICAL) | 0 | 0 | 7 | Both controls verified; 3 true findings beyond the key (added as #12-14); owner paragraph first, 0 questions, 1 decision with options; sweeps.md read in full; vendor manual cited by page; fixes escalated (not a git repo, §0.8). 179k tokens. |
| 2026-09-12 | 3d10504 | live, mixoweb sandbox shop | `probe_mpg.php` × CREDIT WEBATM VACC CVS BARCODE LINEPAY ESUNWALLET + wrong-IV control | 6 PASS (payType=1), ESUNWALLET → MPG02003, wrong IV → MPG03009 | — | 0 | 0 | 2 | First live run of the NewebPay probe. LINE Pay is enabled on this sandbox (the recorded 客服 wait is over). MPG03009 and the payType block added to the classifier from this run. |
| 2026-09-12 | e54fe36 | live, mixoweb sandbox shop | `probe_mpg.php ESUNWALLET` after the owner flipped the console toggle | PASS (payType=1) | — | 0 | 0 | 1 | Corrects the guide: 玉山 Wallet is a sandbox console toggle, not form-gated; toggle→probe loop < 1 min. |
| 2026-09-12 | f242436 | paired (cia + ecommerce-cia), subagent, cold | Screen — "run /cia and /ecommerce-cia together, Screen tier" on fixture-shop as a git repo | 14 | 0 | 0 | 0 | 10 | Both sweeps.md read in full + sweep-diff.py run; one discovery/map/line-per-sweep; no duplicates; cia §3/§7/§8/§0.10 present; owner paragraph first. 2 new true defects (#15 COD orders expired while at the counter, #16 no env switch). TOCTOU graded HIGH by a second independent run → key minimum lowered to HIGH. 245k tokens. |
| 2026-09-12 | 26e26f8 | Claude (subagent, cold) | setup mode, 4 scripted turns (陶器 2000–8000, card+LINE Pay, 7-11/全家 取貨付款, Bluehost→Hetzner) | 7/7 criteria | — | 0 | 0 | 4 | 1 question/turn with choices; LINE Pay form + dated wait; NT$20,000 cap reasoned vs price; S19 table for both hosts, outbound-IP question, "LINE Pay needs no static IP"; ran readiness.py; one bold next action. Gaps it exposed and fixed same day: Q3-bis online+取貨付款 ambiguity, form needs MerchantID so account first, cap citations, Q6 as a card decision, UTF-8 console bug in all tools. 99k tokens. |
| 2026-09-13 | — | live, PAYUNi sandbox, fake credentials | `probe_upp.php Credit` | refusal path exact: `JS_INFO.success=false` 商店不存在 | — | 0 | 0 | 1 | First contact with PAYUNi; brand-name PASS heuristic was wrong and replaced by the JS_INFO verdict. PASS path needs a real sandbox shop. |
| 2026-09-13 | — | vendor known-answer, offline | `probe_mpg.php --selftest`, `callback.php selftest`, `test_newebpay_tools.py` × NDNF-1.2.5 §4.1 example (key, IV, plaintext → `TradeInfo` 448 hex, `TradeSha`; Step-5 signed callback) | reproduced byte-for-byte by PHP and by Python stdlib; tampered vector → selftest red; wrong IV → `SIGNATURE MISMATCH` with nothing decoded | — | 0 | 0 | 2 | First proof that the NewebPay codec is NewebPay's, not merely self-consistent. Two tool defects surfaced by the new tests and fixed: `fetch_manuals.py` did not put the newest manual first; `readiness.py` accepted a `wait` with no who/since date. |
| 2026-09-13 | — | live, LINE Pay sandbox host, fake credentials | `probe_request.php` (v3) | refusal path exact: `returnCode 1104 Merchant not found`, as the reference's result-code table says; `GET` on the request path → `2104` | — | 0 | 0 | 1 | First contact with LINE Pay direct. Reference read at source the same day: Online API v3 **and v4** (v4 since Nov 2025); HMAC message = secret + path + body + nonce; no merchant IP allowlist for the Online API (the allowlist is an Offline API concept). PASS path needs a sandbox Channel ID. |
| 2026-09-13 | — | live, LINE Pay sandbox Channel (owner's) | `probe_request.php` full walk: request (v3 + v4) → simulator PAY NOW → `--check` → `--confirm` → `--details` → `--refund` + retries | PASS ×2; `0110`; confirm `0000` `payInfo CREDIT_CARD 1`; details `CAPTURE`; refund `0000`; re-refund `1165`; re-confirm `1172`; check after `0123` | — | 0 | 0 | 15 | First PASS-path proof for LINE Pay direct. Lessons into the guide §5a: the sandbox payment page is a decoy ("update LINE") over a pop-up simulator with PAY NOW and no LINE login; phone fails, PC works; confirm before `0110` returned `1169` and killed the reservation (`0122`); second confirm is `1172`, not `1145`/`1152`; `--details` before confirm is `1150`. `--confirm/--refund/--details` added to the probe during the run. |
| 2026-09-13 | — | live, LINE Pay sandbox Channel | `probe_request.php --v4 --no-capture` (authorise-only request) | `2103 Parameter is not allowed [capture:false]` | — | 0 | 0 | 1 | Capture-separated is contractual; a sandbox merchant does not have it. Guide §5a + gotchas; `--capture` / `--void` added to the probe for merchants that do. Same hour: the sandbox payment page changed from the pop-up simulator (Shape A) to the LINE web-login / QR page (Shape B) — both documented; Shape B is where the sandbox payer account is used. |
| 2026-09-13 | — | live, PAYUNi sandbox shop (owner's, fresh) | `probe_upp.php` × Credit ATM CVS LinePay ICash Aftee JKoPay ApplePay GooglePay SamsungPay + wrong-IV control | Credit / ATM / CVS PASS (`JS_INFO.success=true`); wrong IV → `Hash比對不符合`; the rest → `UPP02073/49/50/109/63` 此支付工具未啟用，<method>，請聯繫商店 | — | 0 | 0 | 6 | First PASS-path proof for PAYUNi. The not-enabled refusal is an auto-submitting form back to ReturnURL with an encrypted result (HashInfo verified, decrypted) — the probe said UNKNOWN until taught. Guide corrected: LINE Pay is a console toggle a new sandbox shop has off, not "works immediately". Lesson: keys copied by eye fail on l/I/1 — use the console's 複製 buttons. |
| 2026-09-13 | 5c1ab3a | Claude (subagent, cold) | setup mode — empty dir, 「我想用 LINE Pay 收款，怎麼開始？」 | 5/7 criteria | 2 (hard limits not stated at Q1; `fetch_docs.py --latest` not planned) | 0 | 0 | 1 | Read SKILL.md + linepay-onboarding.md in full, ran detect.py; 6-stop road in plain words; direct-vs-via asked first with 4 choices incl. "已在用綠界 → 綠界沒有 LINE Pay"; "不需要固定 IP"; gateway wait described as dated. 81k tokens. |
| 2026-09-13 | — | live, LINE Pay sandbox Channel | `probe_request.php --v4` full walk | request PASS → PAY NOW → `0110` → confirm `0000` `payInfo CREDIT_CARD 1` **`paymentProvider: TSP`** → details `CAPTURE` + provider → refund `0000` | — | 0 | 0 | 3 | v4 differs from v3 only by the provider field, as the change log says. An earlier v4 reservation expired unapproved after ~50 min (`0121`) — guide gotcha. The same reservation URL served the pop-up-simulator shape this time; both shapes documented in §5a. |
| 2026-09-15 | 919531b + the external-lessons edits uncommitted | Claude (subagent, cold) | Screen — `pre-launch audit, Screen tier` on fixture-shop, run #1 | 12 | 2 (S12 store choice filed under S17/S4; S3 PDOException filed under S11) | 2 (#14 ReminderJob `PT{hours-12}` throw; #16 no env switch) | 0 | 9 | **Compromised:** the agent disclosed one grep that printed five key rows (1, 2, 7, 9, 10) into its context after the code had been read; treated as a rehearsal, not the row of record. 216k tokens. |
| 2026-09-15 | 919531b + the external-lessons edits uncommitted | Claude (subagent, cold, key excluded from every grep) | Screen — same request, run #2 | 12 | 3 (#11 S12 store choice filed under S4 at HIGH; #12 S16 row-count HIGH vs CRITICAL; #13 S3 PDOException HIGH vs CRITICAL) | 1 (#14 the `PT{hours-12}` throw — the `reminder_sent_at`-without-a-mail half was filed under S6 at LOW) | 0 | 13 | 11 probe scenarios executed against an in-memory copy of the schema; 5 extra true findings beyond the key (F-10 everything after paid unbuilt, F-16 raw `$post`, F-17 zero contract tests — S24 working, F-19 host unknown, F-20 interface in a class file). Two of three nears are a grade one level low on a money path — the severity floor added to reporting.md the same day. The one miss is a thrown DateInterval on a sub-12 h window: S12 "a job that records an effect it did not produce" was half-found; the throw itself needs the reader to compute `PT{$hours - 12}H` for a small window — noted, no doctrine change. 243k tokens. |
| 2026-09-17 | cf4ec39 (S12.2, S15.3, S16.3, S16.4, S19.2, S20.4, S22.8, S23.2, kitchen-sink, README objections) | Claude (subagent, cold, key excluded, sibling not loaded) | Screen — `pre-launch audit, Screen tier` on fixture-shop | 14 | 1 (#12 S16 row-count HIGH vs CRITICAL) | 1 (#16 S22.6 hardcoded ReturnURL, no sandbox switch) | 0 | 9 | **Up from 12/3/1.** #10 and #11 now filed under S17 and S12 as the key says; #13 under S3; #14 (`PT-6H`) found (F-16); #15 found at CRITICAL (F-14). 6 extra true findings (NT$20,000 cap unenforced, pickup_hold_days dead, S19 five unknowns, no vendor vector, no badge table anywhere, one status column). 224k tokens. |
| 2026-09-19 | cf4ec39 + today's edits uncommitted (walk step (f) layout geometry; S3 Grading paragraph — catch-all around a callback write that answers 200 is CRITICAL; S16 grading names the zero-row callback update as CRITICAL; S20 — an unread heartbeat is no liveness signal, HIGH; S1.1 arithmetic on a configurable value; S22.6 the environment as the largest invisible mode + `environment mode switch:` item in the S22 line; answer key moved out of the fixture to `tests/EXPECTED-fixture-shop.md`) | Claude (subagent, cold, key outside the fixture, no git, sibling not loaded) | Screen — `pre-launch audit, Screen tier` on fixture-shop | 15 | 1 (#15 COD expired by the payment clock at HIGH vs CRITICAL — the run before graded it CRITICAL; S15 has no Grading paragraph, next edit) | 0 | 0 | 12 | **Up from 14/1/1 — first run with no miss.** Four runs today: two leaked the key through the S6 deferred-work grep over `tests/` (both scored 13.5, not recorded); one clean run on the S3/S16/S20 text scored 14/0/2 (misses #14 `PT{hours-12}`, #16 no env switch); this run on the S1.1 + S22.6 text found both. The three standing nears (#6 unread heartbeat, #12 zero-row callback, #13 catch-all PDOException) closed by the grading text. 12 extra true findings incl. lexical DATE_ATOM compare across a DST offset and `GatewayQuery` declared in another class's file. 237k tokens. |
| 2026-09-20 | 0b96d5a + today's edits uncommitted (S5.1 retired-caller script; S13.1 second hand-rolled map; S20.5 walk receipt is a one-day fact; S21 shape 6 runner's file listing; S22.9 button with no handler) | Claude **Sonnet** (subagent, cold, key outside the fixture, git forbidden) | Screen — `pre-launch audit, Screen tier` on fixture-shop | 11 | 2 (S13 refund desk and S12 store-chain ingress both filed under S11) | 3 (S1 interval arithmetic under 12 h; S15 collection≠settlement cell never promoted to a finding; S22.6 applied to one constructor, not enumerated) | 0 (+1 real unplanted: S24 no contract test on the money boundary) | 150 | **Signal row, not the gate row — cheaper auditor model; the row below re-runs the same doctrine on Opus.** Misses are promotion/enumeration failures, not discovery failures. RUNS rows now name the model. |
| 2026-09-20 | 0b96d5a + the same uncommitted edits | Claude **Opus** (subagent, cold, key outside the fixture, git forbidden; prompt adds "every threshold-meeting observation becomes a graded finding") | Screen — `pre-launch audit, Screen tier` on fixture-shop | 15 | 1 (key#14 stamp-without-send found and confirmed, filed under S6 not S1/S12) | 0 | 0 | 15 | **Holds the 15.5 baseline** after five additive doctrine edits; 15 extra correctly-evidenced findings beyond the planted rows (store id never recorded, COD cap, status push 300–303 with no consumer, advancers without heartbeats, ISO-8601 negative interval, UTC-vs-local cutoff, admin templates with zero controls, …) — candidates for planted rows; both controls kept out of the findings. |
